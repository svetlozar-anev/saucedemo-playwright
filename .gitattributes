# Ensure all text files use LF (UNIX-style) line endings in the repo
* text=auto eol=lf

# Treat binary files as binary (no line ending conversion)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.mp4 binary
